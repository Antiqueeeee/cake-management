# 小蛋糕店业务管理系统 · 详细设计

状态：v1.4。v1.0 写最硬、改动成本最高的部分——数据库详细 schema（17 张表的完整字段清单、JSONB 字段结构、索引、枚举、种子数据）；v1.1 按 v1.0 子代理 review 修复 1 处脑补字段（删除 `customer.display_name`，需求文档无此字段）+ 补 1 处 CHECK 约束（`order_status_log` 的 `action = CREATE ↔ old_status IS NULL` 双向约束，防脏数据）+ 5 处文档可读性优化（`menu.shelf_life_days` 加前端 UX 提示、`order_status_log` 段补 CREATE 与 created_at 语义说明、`recipe_snapshot.menu_id` 补与 `order.menu_id` 关系说明、§1.3.1 `change_diff` 示例补「本次字段」diff、§1.7 第 4 条补 lifecycle.py 对应）；v1.2 按 v1.1 子代理 review 必修 1 项（§1.3.1 `change_diff` 示例补全 7 类可改字段，原缺 `menu_id` / `pickup_method`，与需求文档第二章故事 5 §311 不一致）；v1.3 追加 §2 订单状态机（7 状态、ASCII 转换图、8 个 ORDER_ACTION 转换规则、状态约束矩阵、第一期不实现/第二期扩展对比）；**v1.4 追加 §3 API 契约概览**（契约生成与同步机制、路由模块化组织、Pydantic schema 分层、通用响应包装、列表 query 约定、错误码命名规范、WebSocket 事件协议、内部回写端点）**+ §4 前端页面与 ASCII 线框**（后端管理页面 10 个页面：登录/主框架/订单列表/订单详情/改单弹窗/菜单/原料/配方/采购/客户；后厨屏 5 个页面：登录/看板/配方查看/取货关闭/送达关闭；通用约定：API 客户端封装、操作人字段统一来源、提示音与 Wake Lock 设置面板）。API 详细字段不写在文档里，由后端 Pydantic 自动生成 OpenAPI、前端 `openapi-typescript` 同步类型，文档只写设计准则与约定。后续 v1.5 补 §5 E2E 测试策略，v1.6 补 §6 第一刀切片任务分解与 §7 开发顺序建议。
最后更新：2026-06-29
配套文档：[需求文档.md](./需求文档.md) / [技术方案.md](./技术方案.md) / [时序图.md](./时序图.md)

---

## 0. 文档说明

### 0.1 本文档的位置

| 文档 | 回答的问题 | 读者 |
|---|---|---|
| 需求文档 | 业务要什么、验收标准是什么 | 业主、产品、开发 |
| 技术方案 | 为什么这么选（FastAPI 不选 Django…）、整体架构、模块怎么分 | 开发、架构评审 |
| **详细设计（本文档）** | **具体怎么落地：每个表每个字段、每个状态怎么转、每个页面长啥样** | **即将开工的开发者** |

简单说：详细设计是「写代码的依据」。开发者拿到这份文档 + 技术方案 + 需求文档，应该不用再问任何业务/技术决策细节就能动手。

### 0.2 v1.0 范围

v1.0 只写**数据库详细 schema**（§1）。理由：ORM 模型、Alembic 迁移、service 层业务逻辑都建立在字段清单之上；字段定错了，所有上层建筑都要返工。这是开工前最关键的依赖。

后续版本计划：
- v1.1：§2 状态机（订单状态转换图、触发角色、联动动作）
- v1.2：§4 前端页面与 ASCII 线框（API 详细契约不写——Q1 已确认让 Pydantic 自动生成 OpenAPI、前端 `openapi-typescript` 反生成类型）
- v1.3：§5 第一刀切片任务分解 + §6 开发顺序建议

### 0.3 与技术方案、需求文档的关系

- **技术方案 §4**：给出表清单、关键约束、关键索引（约束级）
- **需求文档各章 §2/§3**：给出业务字段定义（业务级）
- **本文档 §1**：把两者结合，给出可落地的字段清单（落地级）

不冲突：技术方案不重新写字段清单，本文档不重新论证选型与约束。

**冲突时如何裁决**：
- 本文档 ↔ 技术方案冲突：以技术方案为准（约束层高于实现层）
- 本文档 ↔ 需求文档冲突：业务含义以需求文档为准；命名/类型选择以本文档为准（落地选择是本文档职责）

### 0.4 设计约定

**命名**
- 表名 / 字段名：snake_case
- 主键：统一用 `<表名单数>_id`（如 `order_id`、`customer_id`），自增 BIGINT
- 外键：`<引用表单数>_id`
- 时间字段：`*_at` 后缀（`created_at`、`updated_at`、`last_login_at`），类型 TIMESTAMPTZ
- 布尔字段：`is_*` 或 `has_*` 前缀
- 枚举字段：PostgreSQL ENUM 类型，值大写下划线（如 `ORDER_STATUS.PENDING`）

**类型缩写**
- `BIGINT`：64 位整数（主键、外键）
- `INT`：32 位整数（小范围计数）
- `TEXT`：变长字符串
- `NUMERIC(p, s)`：定点数，p 总位数、s 小数位
- `BOOLEAN`：布尔
- `TIMESTAMPTZ`：带时区时间戳
- `DATE`：日期（无时间）
- `JSONB`：二进制 JSON
- `ENUM`：PostgreSQL 枚举类型
- `TEXT[]`：文本数组

**通用字段**（每张表都有，下文表内不再重复说明）
- `<表名单数>_id`：主键，BIGINT NOT NULL，自增
- `created_at`：创建时间，TIMESTAMPTZ NOT NULL DEFAULT now()，不可变
- `updated_at`：更新时间，TIMESTAMPTZ NOT NULL DEFAULT now()，由触发器或 ORM 自动维护

**外键策略**
- 默认 `ON DELETE RESTRICT`（防止误删导致历史留痕悬空）
- 留痕表（`order_status_log`、`ingredient_stock_log`、`bot_conversation_log`）的外键也用 `RESTRICT`——即便关联业务记录被删，留痕必须保留可追溯
- 例外：子表（如 `customer_phone` → `customer`）用 `ON DELETE CASCADE`（父删则子删，避免孤儿数据）

---

## 1. 数据库详细 schema

### 1.1 全表清单（17 张，按模块分组）

| 模块 | 表名 | 用途 | 关键约束 |
|---|---|---|---|
| 菜单 | `menu` | 商品档案 | `name` UNIQUE |
| 菜单 | `menu_global_config` | 全局配置（临期阈值） | 单行表（CHECK config_id = 1） |
| 原料 | `ingredient` | 原料档案 + 当前库存（合并设计） | `name` UNIQUE；`current_stock` 允许负 |
| 原料 | `ingredient_stock_log` | 库存变动留痕 | 每次变动一行 |
| 配方 | `recipe` | 一商品一配方 | `menu_id` UNIQUE |
| 配方 | `recipe_ingredient` | 配方-原料关联 | `(recipe_id, ingredient_id)` UNIQUE |
| 配方 | `recipe_snapshot` | 制作中锁定的配方快照 | `order_id` UNIQUE |
| 客户 | `customer` | 客户主档 | `wxid` UNIQUE |
| 客户 | `customer_phone` | 电话列表 | `(customer_id, phone)` UNIQUE |
| 客户 | `customer_address` | 地址列表 | `(customer_id, address)` UNIQUE |
| 客户 | `customer_note` | 备注列表 | - |
| 订单 | `order` | 订单主表 | `(order_date, daily_seq)` UNIQUE |
| 订单 | `order_status_log` | 订单状态变更留痕 | 含 `change_diff` JSONB |
| 采购 | `purchase` | 采购登记 | - |
| 鉴权 | `user` | PIN 登录用户 | `pin_hash` 不加索引不加 UNIQUE |
| 鉴权 | `api_token` | 程序 Bearer Token | `token_hash` UNIQUE |
| 鉴权 | `bot_conversation_log` | 机器人对话留痕 | 5 类 `step_type` |

### 1.2 字段清单

> 通用字段（`<表>_id` / `created_at` / `updated_at`）按 §0.4 约定默认存在，下列各表为节省篇幅省略部分通用说明，但实际表结构都包含。

#### 1.2.1 菜单模块

##### `menu`（商品档案）

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| menu_id | BIGINT | NO | - | 主键 |
| name | TEXT | NO | - | 菜名，UNIQUE；需求文档第四章 §2-(1) |
| price | NUMERIC(10, 2) | NO | - | 售价（元） |
| shelf_life_days | INT | NO | - | 保质期（天）；用于成品库存临期计算（**第二期启用，第一期字段就位**）；第一期店主录入菜单时必填但系统不消费此字段，**前端 v1.2 录入表单须加 tooltip 说明第二期用途、避免店主困惑** |
| is_customizable | BOOLEAN | NO | FALSE | 是否支持定制；**第一期预留不参与业务**，第二期 DIY 模块上线后启用（需求文档第十二章 §5） |
| is_active | BOOLEAN | NO | TRUE | 是否在售；下架商品不能被新订单引用 |
| created_at | TIMESTAMPTZ | NO | now() | |
| updated_at | TIMESTAMPTZ | NO | now() | |

**索引**：UNIQUE(name)

##### `menu_global_config`（全局配置）

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| config_id | BIGINT | NO | - | 主键；应用层保证只有 1 行（id = 1） |
| expiry_threshold_ratio | NUMERIC(3, 2) | NO | 0.20 | 临期阈值比例（0.20 = 20%），需求文档第四章 §3 |
| updated_at | TIMESTAMPTZ | NO | now() | |
| updated_by | BIGINT | NO | - | FK → user.user_id；最近修改人 |

**约束**：CHECK (config_id = 1)，单行表防止误插入多行。

---

#### 1.2.2 原料模块

##### `ingredient`（原料档案 + 当前库存）

需求文档第五章 §2（档案字段）与 §3（库存字段）合并为一张表——库存当前值与档案 1:1，独立成表徒增 join。

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| ingredient_id | BIGINT | NO | - | 主键 |
| name | TEXT | NO | - | 原料名，UNIQUE；需求文档第五章 §2-(1) |
| unit | TEXT | NO | - | 计量单位（克/毫升/个/袋）；采购与配方共用 |
| unit_price | NUMERIC(10, 2) | YES | NULL | 最近采购平均单价；无采购记录时为空 |
| safety_stock | NUMERIC(10, 2) | NO | 0 | 安全库存线；低于此值提醒补货，0 = 不提醒 |
| shelf_life_days | INT | YES | NULL | 原料保质期（天）；**仅作记录**，原料不按批次管理、不计算临期（需求文档第五章 §3 末段） |
| storage_location | TEXT | YES | NULL | 存放位置（货架号、冰柜号） |
| common_source | TEXT | YES | NULL | 常用购买来源 |
| current_stock | NUMERIC(10, 2) | NO | 0 | 当前库存；**允许负数**（负数绝对值 = 欠缺量） |
| last_restocked_at | TIMESTAMPTZ | YES | NULL | 最近入库时间 |
| created_at | TIMESTAMPTZ | NO | now() | |
| updated_at | TIMESTAMPTZ | NO | now() | |

**索引**：UNIQUE(name)；BTREE(safety_stock)（低库存预警查询用：WHERE current_stock < safety_stock）

##### `ingredient_stock_log`（库存变动留痕）

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| log_id | BIGINT | NO | - | 主键 |
| ingredient_id | BIGINT | NO | - | FK → ingredient；ON DELETE RESTRICT |
| change_amount | NUMERIC(10, 2) | NO | - | 本次变动量（正入库 / 负扣减）；CHECK ≠ 0 |
| new_balance | NUMERIC(10, 2) | NO | - | 变动后余额；与 ingredient.current_stock 同步 |
| source_type | STOCK_SOURCE | NO | - | ENUM：PURCHASE / FINISHED_GOODS_DEDUCT / LOSS / INVENTORY_ADJUSTMENT |
| reason | TEXT | NO | - | 必填原因（需求文档第五章 §4 要求） |
| order_id | BIGINT | YES | NULL | FK → `order`；source_type = FINISHED_GOODS_DEDUCT 时关联订单 |
| purchase_id | BIGINT | YES | NULL | FK → purchase；source_type = PURCHASE 时关联采购 |
| operator_user_id | BIGINT | NO | - | FK → user；操作人留痕 |
| created_at | TIMESTAMPTZ | NO | now() | |

**索引**：BTREE(ingredient_id, created_at)；BTREE(source_type)

**约束**：
- CHECK (change_amount <> 0)
- CHECK (
    (source_type = 'FINISHED_GOODS_DEDUCT' AND order_id IS NOT NULL) OR
    (source_type = 'PURCHASE' AND purchase_id IS NOT NULL) OR
    source_type IN ('LOSS', 'INVENTORY_ADJUSTMENT')
  )

**注意**：按技术方案 §4.2，**订单取消不写本表**——原料消耗在完工时已扣且不可回滚，取消订单时原料账面不再调整。

---

#### 1.2.3 配方模块

##### `recipe`（配方档案）

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| recipe_id | BIGINT | NO | - | 主键 |
| menu_id | BIGINT | NO | - | FK → menu；UNIQUE（一商品一配方） |
| steps | JSONB | NO | - | 制作步骤数组，结构见 §1.3.4 |
| total_time_minutes | INT | YES | NULL | 总制作时长；可从 steps 推导，存了查询方便 |
| equipment | TEXT[] | NO | '{}' | 适用设备（烤箱、打蛋器等） |
| loss_rate | NUMERIC(3, 2) | NO | 0 | 损耗率（0.05 = 5%） |
| allergens | TEXT[] | NO | '{}' | 过敏原列表（坚果/乳制品/麸质等） |
| created_at | TIMESTAMPTZ | NO | now() | |
| updated_at | TIMESTAMPTZ | NO | now() | |

**索引**：UNIQUE(menu_id)

##### `recipe_ingredient`（配方-原料关联）

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| recipe_ingredient_id | BIGINT | NO | - | 主键 |
| recipe_id | BIGINT | NO | - | FK → recipe；ON DELETE CASCADE |
| ingredient_id | BIGINT | NO | - | FK → ingredient；ON DELETE RESTRICT |
| quantity | NUMERIC(10, 2) | NO | - | 用量（按 ingredient.unit） |
| created_at | TIMESTAMPTZ | NO | now() | |

**索引**：UNIQUE(recipe_id, ingredient_id)；BTREE(ingredient_id)（反查：哪些配方用了此原料——配方修改影响查询用）

##### `recipe_snapshot`（订单配方快照）

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| snapshot_id | BIGINT | NO | - | 主键 |
| order_id | BIGINT | NO | - | FK → `order`；UNIQUE（一订单一快照） |
| menu_id | BIGINT | NO | - | FK → menu；快照时点的商品引用。**与 `order.menu_id` 在快照生成那一刻应一致**，独立存是为表达「制作时点的商品」语义、便于按商品统计订单 |
| content | JSONB | NO | - | 完整配方内容（含原料清单、步骤、损耗率、过敏原），结构见 §1.3.2 |
| created_at | TIMESTAMPTZ | NO | now() | |

**索引**：UNIQUE(order_id)

**写入时机**：订单状态从 PENDING → COOKING（开始制作）时由 `lifecycle.py` 触发锁定（技术方案 §4.2）；完工扣料按此快照口径。

---

#### 1.2.4 客户档案模块

##### `customer`（客户主档）

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| customer_id | BIGINT | NO | - | 主键 |
| wxid | TEXT | NO | - | 微信 wxid，UNIQUE；需求文档第三章 §2-(1) |
| last_order_at | TIMESTAMPTZ | YES | NULL | 最近下单时间；建档时为空，下单后更新 |
| created_at | TIMESTAMPTZ | NO | now() | |
| updated_at | TIMESTAMPTZ | NO | now() | |

**索引**：UNIQUE(wxid)

##### `customer_phone`（电话列表）

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| phone_id | BIGINT | NO | - | 主键 |
| customer_id | BIGINT | NO | - | FK → customer；ON DELETE CASCADE |
| phone | TEXT | NO | - | 电话号码 |
| label | TEXT | YES | NULL | 标签（如「家庭」「单位」「自取」） |
| created_at | TIMESTAMPTZ | NO | now() | |

**索引**：UNIQUE(customer_id, phone)

##### `customer_address`（地址列表）

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| address_id | BIGINT | NO | - | 主键 |
| customer_id | BIGINT | NO | - | FK → customer；ON DELETE CASCADE |
| address | TEXT | NO | - | 地址 |
| label | TEXT | YES | NULL | 标签（如「家」「单位」） |
| created_at | TIMESTAMPTZ | NO | now() | |

**索引**：UNIQUE(customer_id, address)

##### `customer_note`（备注列表）

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| note_id | BIGINT | NO | - | 主键 |
| customer_id | BIGINT | NO | - | FK → customer；ON DELETE CASCADE |
| content | TEXT | NO | - | 备注内容（偏好式信息，需求文档第三章 §2-(4)） |
| source | NOTE_SOURCE | NO | - | ENUM：MANUAL（店员手填）/ BOT_EXTRACTED（机器人从订单消息提取） |
| created_at | TIMESTAMPTZ | NO | now() | |

**索引**：BTREE(customer_id, created_at)

---

#### 1.2.5 订单模块

##### `order`（订单主表）

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| order_id | BIGINT | NO | - | 主键；内部 ID（需求文档第二章 §3-(2)） |
| order_date | DATE | NO | - | 业务日期；用于编号生成与跨年查询 |
| daily_seq | INT | NO | - | 当日流水；从 1 起，参与编号生成 |
| customer_id | BIGINT | NO | - | FK → customer |
| menu_id | BIGINT | NO | - | FK → menu；引用商品必须在售（is_active = TRUE） |
| quantity | INT | NO | - | 数量；CHECK > 0 |
| pickup_at | TIMESTAMPTZ | NO | - | 取货/送达时间 |
| pickup_method | PICKUP_METHOD | NO | - | ENUM：PICKUP / DELIVERY |
| contact_phone | TEXT | NO | - | 本次联系电话；不关联 customer_phone，本次可临时改 |
| delivery_address | TEXT | YES | NULL | 本次配送地址；**pickup_method = DELIVERY 时必填**，自取时为 NULL |
| order_note | TEXT | YES | NULL | 本次订单备注；与 customer_note 独立维护 |
| payment_status | PAYMENT_STATUS | NO | 'UNPAID' | ENUM：UNPAID / PAID |
| payment_method | PAYMENT_METHOD | YES | NULL | ENUM：WECHAT / CASH / OTHER；**payment_status = PAID 时必填** |
| status | ORDER_STATUS | NO | 'PENDING' | ENUM：PENDING / COOKING / READY_FOR_PICKUP / READY_FOR_DELIVERY / DELIVERING / COMPLETED / CANCELLED |
| price | NUMERIC(10, 2) | NO | - | 订单总金额；等于 menu.price × quantity（应用层或生成列） |
| recipe_snapshot_id | BIGINT | YES | NULL | FK → recipe_snapshot；**status 进入 COOKING 后填**（技术方案 §4.2） |
| operator_user_id | BIGINT | YES | NULL | FK → user；浏览器端创建订单时填；机器人下单时为 NULL（操作人由 operator_wxid 在 bot_conversation_log 留痕替代） |
| created_at | TIMESTAMPTZ | NO | now() | 下单时间（精确到秒） |
| updated_at | TIMESTAMPTZ | NO | now() | |

**索引**：
- UNIQUE(order_date, daily_seq)：编号生成的并发安全（技术方案 §4.4）
- BTREE(status)：后厨屏按状态过滤
- BTREE(customer_id, created_at)：补录重复检测
- BTREE(pickup_at, status)：后厨今日看板按取货时间排序

**编号生成**（display_no）：**不存字段**，由 SQL 表达式或 ORM property 派生——`to_char(order_date, 'MMDD') || '-' || lpad(daily_seq::TEXT, 3, '0')`，例如 `0629-001`。单一可信源（display_no 是 order_date + daily_seq 的函数，存了易不一致）。

**约束**：
- CHECK (quantity > 0)
- CHECK (
    (pickup_method = 'DELIVERY' AND delivery_address IS NOT NULL) OR
    pickup_method = 'PICKUP'
  )
- CHECK (
    (payment_status = 'PAID' AND payment_method IS NOT NULL) OR
    payment_status = 'UNPAID'
  )

##### `order_status_log`（订单状态变更留痕）

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| log_id | BIGINT | NO | - | 主键 |
| order_id | BIGINT | NO | - | FK → `order`；ON DELETE RESTRICT |
| old_status | ORDER_STATUS | YES | NULL | 旧状态；首次创建订单时为 NULL |
| new_status | ORDER_STATUS | NO | - | 新状态 |
| action | ORDER_ACTION | NO | - | ENUM：CREATE / UPDATE / START_COOKING / FINISH / MARK_DISPATCHED / CLOSE_PICKUP / CLOSE_DELIVERY / CANCEL |
| change_diff | JSONB | YES | NULL | 改单前后字段级 diff；仅 action = UPDATE 时填，结构见 §1.3.1 |
| note | TEXT | YES | NULL | 自由文本说明（取消原因等） |
| operator_user_id | BIGINT | YES | NULL | FK → user；浏览器端操作时填 |
| operator_wxid | TEXT | YES | NULL | 机器人端操作时填；与 operator_user_id 二选一 |
| created_at | TIMESTAMPTZ | NO | now() | |

**索引**：BTREE(order_id, created_at)

**约束**：
- CHECK (
    (operator_user_id IS NOT NULL AND operator_wxid IS NULL) OR
    (operator_user_id IS NULL AND operator_wxid IS NOT NULL)
  )
- CHECK (
    (action = 'CREATE' AND old_status IS NULL) OR
    (action <> 'CREATE' AND old_status IS NOT NULL)
  )——CREATE 是状态机入口（无 → PENDING）、无旧状态；其他 7 种 action 必须有旧状态

**注意**：
- 所有状态变更必须经 service 层事务（更新 `order.status` + 写本表 + 触发库存联动 + WebSocket 推送），禁止直接改 `status`（技术方案 §4.3）
- `change_diff` 与 `note` 分工：前者记结构化字段变更，后者记自由文本
- **`action = CREATE` 行与 `order.created_at` 互补**：`created_at` 只记时间、无操作人语义；CREATE 行额外承载「谁创建」（含机器人下单时的 `operator_wxid`），是机器人留痕的关键一环，不冗余

---

#### 1.2.6 采购模块

##### `purchase`（采购登记）

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| purchase_id | BIGINT | NO | - | 主键 |
| ingredient_id | BIGINT | NO | - | FK → ingredient |
| quantity | NUMERIC(10, 2) | NO | - | 采购数量；CHECK > 0 |
| unit_price | NUMERIC(10, 2) | NO | - | 单价 |
| total_amount | NUMERIC(10, 2) | NO | - | 总金额 = quantity × unit_price（应用层或生成列） |
| source | TEXT | YES | NULL | 购买来源 |
| operator_user_id | BIGINT | NO | - | FK → user |
| created_at | TIMESTAMPTZ | NO | now() | |

**索引**：BTREE(ingredient_id, created_at)；BTREE(created_at)

**联动**（service 层事务）：采购登记时同事务——
1. `ingredient.current_stock += quantity`
2. `ingredient.unit_price = unit_price`（最近采购单价覆盖）
3. `ingredient.last_restocked_at = now()`
4. 写一条 `ingredient_stock_log`（source_type = PURCHASE）

---

#### 1.2.7 鉴权模块

##### `user`（PIN 登录用户）

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| user_id | BIGINT | NO | - | 主键 |
| pin_hash | TEXT | NO | - | bcrypt 哈希；**不加 UNIQUE 不加索引**（bcrypt 同明文不同 hash，无法 SELECT 匹配，登录靠遍历 checkpw，详见技术方案 §4.2） |
| display_name | TEXT | NO | - | 显示名（如「店主」「店员张三」「后厨李四」）；写入所有留痕表的「操作人」字段 |
| role | USER_ROLE | NO | - | ENUM：OWNER / STAFF / KITCHEN |
| enabled | BOOLEAN | NO | TRUE | 账号启用开关 |
| failed_attempts | INT | NO | 0 | PIN 错误次数；累计 5 触发锁定 |
| locked_until | TIMESTAMPTZ | YES | NULL | 锁定截止时间；NULL = 未锁 |
| last_login_at | TIMESTAMPTZ | YES | NULL | 最近登录时间 |
| created_at | TIMESTAMPTZ | NO | now() | |
| updated_at | TIMESTAMPTZ | NO | now() | |

**索引**：BTREE(display_name)；部分索引 `WHERE enabled = TRUE`（登录遍历时只扫启用用户）

**约束**：CHECK (failed_attempts >= 0 AND failed_attempts <= 5)

##### `api_token`（程序 Bearer Token）

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| token_id | BIGINT | NO | - | 主键 |
| token_hash | TEXT | NO | - | bcrypt 哈希；UNIQUE（与 pin_hash 不同——token 校验是先 hash 再 SELECT 比对，可加索引） |
| name | TEXT | NO | - | 凭证名称（如「OpenClaw 主机」） |
| enabled | BOOLEAN | NO | TRUE | 启用开关 |
| last_used_at | TIMESTAMPTZ | YES | NULL | 最近使用时间 |
| created_at | TIMESTAMPTZ | NO | now() | |

**索引**：UNIQUE(token_hash)

**与 `user` 表的区别**（技术方案 §4.2）：user 给人登录（有 PIN 锁定逻辑），api_token 给程序调用（无锁定逻辑、泄露靠轮换）；凭证类型不同、语义不应混淆。

##### `bot_conversation_log`（机器人对话留痕）

| 列名 | 类型 | 可空 | 默认 | 约束 / 说明 |
|---|---|---|---|---|
| log_id | BIGINT | NO | - | 主键 |
| session_id | TEXT | NO | - | 一次接单流程的多次交互聚合；由 OpenClaw Plugin 从 `ctx.sessionKey` 派生 |
| step_type | CONVERSATION_STEP | NO | - | ENUM：CUSTOMER_ORIGINAL_MSG / RECOGNITION_RESULT / BOT_REPLY / CUSTOMER_REPLY / ORDER_PERSISTED；对应需求文档第九章 §4-(5) 五类痕迹 |
| payload | JSONB | NO | - | 该步骤内容，按 step_type 不同结构不同，见 §1.3.3 |
| operator_wxid | TEXT | YES | NULL | 店员侧留痕来源；前四类填，第五类（订单入库）可不填（已有 order_id 关联） |
| order_id | BIGINT | YES | NULL | FK → `order`；仅 step_type = ORDER_PERSISTED 时填，回填关联订单 |
| created_at | TIMESTAMPTZ | NO | now() | |

**索引**：
- BTREE(session_id, created_at)：按接单会话追溯完整对话流
- BTREE(operator_wxid, created_at)：按店员查历史接单痕迹
- 部分索引 `WHERE order_id IS NOT NULL`：按订单查关联对话

**写入路径**（技术方案 §4.2 + §3.4）：
- 前四类：OpenClaw Plugin 注册 `message_received` / `message_sent` / `before_tool_call` hook，POST 到 `/api/internal/bot-conversation-log`
- 第五类：MCP 工具 `create_order` 在 service 层事务中直接写入（与订单入库同事务）

---

### 1.3 JSONB 字段结构

#### 1.3.1 `order_status_log.change_diff`

改单（action = UPDATE）前后的字段级 diff，**仅记实际变更字段**、未变字段不记。下列示例覆盖需求文档第二章故事 5 §311 列出的**全部 7 类可改字段**：商品(`menu_id`) / 数量(`quantity`) / 取货方式(`pickup_method`) / 取货时间(`pickup_at`) / 联系电话(`contact_phone`) / 配送地址(`delivery_address`) / 备注(`order_note`)。`menu_id` 的 diff 内嵌 `name` 字段便于人读、防事后追溯时菜单已改名；实际改单通常只动 1-3 个字段，本例为示意合并展示：

```json
{
  "menu_id": {"old": {"id": 5, "name": "蓝莓蛋糕"}, "new": {"id": 7, "name": "芒果蛋糕"}},
  "quantity": {"old": 1, "new": 2},
  "pickup_method": {"old": "PICKUP", "new": "DELIVERY"},
  "pickup_at": {
    "old": "2026-06-29T15:00:00+08:00",
    "new": "2026-06-29T16:00:00+08:00"
  },
  "contact_phone": {"old": "138xxxx", "new": "139xxxx"},
  "delivery_address": {"old": null, "new": "中关村大街 1 号"},
  "order_note": {"old": null, "new": "加巧克力粉"}
}
```

**字段类型说明**：
- `menu_id` 用 `{id, name}` 嵌套（既可查询又可人读）
- `pickup_method` 用枚举字符串（`PICKUP` / `DELIVERY`）
- `delivery_address` 在自取改配送时通常 `old = null`、配送改自取时 `new = null`

**字段级 diff 的好处**：回执可以渲染「数量从 1 改为 2、取货时间延后 1 小时」；事后审计可精确追溯哪次改了哪个字段。

**与 `note` 字段分工**：`note` 记取消原因等自由文本，`change_diff` 记结构化字段变更，互不重叠。

#### 1.3.2 `recipe_snapshot.content`

订单进入「制作中」时锁定的完整配方内容：

```json
{
  "menu_name": "巧克力蛋糕",
  "steps": [
    {"step_no": 1, "description": "打发蛋白至硬性发泡", "duration_minutes": 10},
    {"step_no": 2, "description": "烘焙 180°C 30 分钟", "duration_minutes": 30}
  ],
  "total_time_minutes": 40,
  "loss_rate": 0.05,
  "allergens": ["乳制品", "麸质"],
  "ingredients": [
    {"ingredient_id": 12, "name": "低筋面粉", "quantity": 200, "unit": "克"},
    {"ingredient_id": 18, "name": "黑巧克力", "quantity": 150, "unit": "克"}
  ]
}
```

**为什么快照**：配方随时可改，但订单按制作那一刻的配方扣料、按当时的损耗率算成本；快照确保扣料口径不被后续配方修改污染。

#### 1.3.3 `bot_conversation_log.payload`

按 step_type 不同结构不同。JSONB 不强约束子结构（不同 step_type 不同结构），由应用层保证写入格式正确：

```json
// step_type = CUSTOMER_ORIGINAL_MSG（店员原始消息）
{"text": "小张订1个巧克力蛋糕，明天下午3点取，电话138xxxx"}

// step_type = RECOGNITION_RESULT（识别结果）
{
  "customer_wxid": "wxid_abc123",
  "menu_name": "巧克力蛋糕",
  "quantity": 1,
  "pickup_at": "2026-06-30T15:00:00+08:00",
  "contact_phone": "138xxxx",
  "missing_fields": []
}

// step_type = BOT_REPLY（机器人回执）
{"text": "已下单，订单号 0629-001"}

// step_type = CUSTOMER_REPLY（店员回复）
{"text": "是的，按这个"}

// step_type = ORDER_PERSISTED（最终入库订单）
{"order_id": 42, "order_no": "0629-001"}
```

#### 1.3.4 `recipe.steps`

配方制作步骤数组（同时作为 `recipe.steps` 字段结构与 `recipe_snapshot.content.steps` 元素结构）：

```json
[
  {"step_no": 1, "description": "打发蛋白至硬性发泡", "duration_minutes": 10},
  {"step_no": 2, "description": "烘焙 180°C 30 分钟", "duration_minutes": 30},
  {"step_no": 3, "description": "出炉冷却", "duration_minutes": 15}
]
```

每步含序号、描述、可选耗时（需求文档第六章 §2-(3)(4)）。`recipe.total_time_minutes` 可由各步 `duration_minutes` 求和得出，存了便于直接查询。

---

### 1.4 索引清单（汇总）

| 表 | 索引字段 | 类型 | 用途 |
|---|---|---|---|
| menu | (name) | UNIQUE | 菜名唯一 |
| menu_global_config | (config_id) | PK + CHECK | 单行表 |
| ingredient | (name) | UNIQUE | 原料名唯一 |
| ingredient | (safety_stock) | BTREE | 低库存预警查询 |
| ingredient_stock_log | (ingredient_id, created_at) | BTREE | 留痕查询（按原料+时间） |
| ingredient_stock_log | (source_type) | BTREE | 按来源筛选 |
| recipe | (menu_id) | UNIQUE | 一商品一配方 |
| recipe_ingredient | (recipe_id, ingredient_id) | UNIQUE | 联合唯一 |
| recipe_ingredient | (ingredient_id) | BTREE | 反查：哪些配方用了此原料 |
| recipe_snapshot | (order_id) | UNIQUE | 一订单一快照 |
| customer | (wxid) | UNIQUE | 客户唯一 |
| customer_phone | (customer_id, phone) | UNIQUE | 防重复电话 |
| customer_address | (customer_id, address) | UNIQUE | 防重复地址 |
| customer_note | (customer_id, created_at) | BTREE | 按客户查备注 |
| `order` | (order_date, daily_seq) | UNIQUE | 编号并发安全 |
| `order` | (status) | BTREE | 后厨屏按状态过滤 |
| `order` | (customer_id, created_at) | BTREE | 补录重复检测 |
| `order` | (pickup_at, status) | BTREE | 后厨今日看板排序 |
| order_status_log | (order_id, created_at) | BTREE | 留痕查询 |
| purchase | (ingredient_id, created_at) | BTREE | 按原料查采购 |
| purchase | (created_at) | BTREE | 按时间查采购 |
| user | (display_name) | BTREE | 按操作人查留痕 |
| user | (enabled) WHERE enabled = TRUE | PARTIAL | 登录遍历只扫启用用户 |
| api_token | (token_hash) | UNIQUE | Bearer Token 校验 |
| bot_conversation_log | (session_id, created_at) | BTREE | 按会话追溯 |
| bot_conversation_log | (operator_wxid, created_at) | BTREE | 按店员查历史 |
| bot_conversation_log | (order_id) WHERE order_id IS NOT NULL | PARTIAL | 按订单查关联对话 |

**与技术方案 §4.4 的关系**：技术方案列了 9 条关键索引（约束级），本表是其完整落地版（增加 UNIQUE/BTREE/PARTIAL 等具体类型，补充技术方案未列的查询索引）。

---

### 1.5 枚举类型清单

| 类型名 | 取值 | 用于字段 |
|---|---|---|
| ORDER_STATUS | PENDING / COOKING / READY_FOR_PICKUP / READY_FOR_DELIVERY / DELIVERING / COMPLETED / CANCELLED | order.status, order_status_log.old_status, order_status_log.new_status |
| ORDER_ACTION | CREATE / UPDATE / START_COOKING / FINISH / MARK_DISPATCHED / CLOSE_PICKUP / CLOSE_DELIVERY / CANCEL | order_status_log.action |
| PICKUP_METHOD | PICKUP / DELIVERY | order.pickup_method |
| PAYMENT_STATUS | UNPAID / PAID | order.payment_status |
| PAYMENT_METHOD | WECHAT / CASH / OTHER | order.payment_method |
| STOCK_SOURCE | PURCHASE / FINISHED_GOODS_DEDUCT / LOSS / INVENTORY_ADJUSTMENT | ingredient_stock_log.source_type |
| USER_ROLE | OWNER / STAFF / KITCHEN | user.role |
| NOTE_SOURCE | MANUAL / BOT_EXTRACTED | customer_note.source |
| CONVERSATION_STEP | CUSTOMER_ORIGINAL_MSG / RECOGNITION_RESULT / BOT_REPLY / CUSTOMER_REPLY / ORDER_PERSISTED | bot_conversation_log.step_type |

**第二期扩展**（仅记录，不在本期实现）：STOCK_SOURCE 增加 `FINISHED_GOODS_RESTOCK`（成品库存备货入库）与 `FINISHED_GOODS_MATCHED_DEDUCT`（订单匹配扣减）两个取值，对应需求文档第十三章第 4 节取舍 1 的两阶段拆分；新增 `spot_sale` 等表对应第七、八章。

---

### 1.6 数据库初始化种子数据

首次部署时预置：

| 表 | 种子数据 | 备注 |
|---|---|---|
| menu_global_config | 1 行：`(config_id=1, expiry_threshold_ratio=0.20, updated_by=<店主 user_id>)` | 默认临期阈值 20% |
| user | 1 行：店主默认账号 `(display_name='店主', role='OWNER', pin_hash=<bcrypt('000000')>, enabled=TRUE)` | **默认 PIN 000000，部署后必须强制改**；种子迁移会在日志中 WARN 提醒 |
| 其他表 | 空 | 业务数据从空开始 |

**实现方式**：通过 Alembic 数据迁移写入（不依赖手工 SQL）。种子迁移单独命名（如 `seed_initial_data`）便于区分表结构变更与数据初始化。

**部署后强制改 PIN 的实现**：首次登录（last_login_at IS NULL）时，前端强制弹出改 PIN 弹窗、不允跳过；改 PIN 后 last_login_at 写入、后续不再强制。具体逻辑在 §2 状态机与 §4 前端线框中展开。

---

### 1.7 与技术方案 §4 / 需求文档的细化说明

本次落地过程中的几处细化（不冲突、仅补充）：

1. **`order.order_date + daily_seq` 拆分**：技术方案 §4.2 说「下单日期 + 当日编号联合唯一索引」，本设计拆为 DATE + INT 两个字段，便于编号生成与查询；display_no（如「0629-001」）不存字段、由两字段派生（单一可信源）
2. **`ingredient` 表合并原料档案与当前库存**：需求文档第五章 §2 是档案字段、§3 是库存字段，技术方案 §4.2 也是合并设计，本设计沿用——`current_stock` 是当前值、`ingredient_stock_log` 是变动留痕，二者配合
3. **`bot_conversation_log.payload` 用 JSONB 不分子表**：5 类痕迹结构差异大、JSONB 灵活度高；如未来查询性能成问题再考虑拆表
4. **`order_status_log.action` 用枚举**：技术方案 §4.2 只说「触发动作」，本设计细化为 8 种枚举（CREATE/UPDATE/START_COOKING/FINISH/MARK_DISPATCHED/CLOSE_PICKUP/CLOSE_DELIVERY/CANCEL）。**8 个枚举值与技术方案 §2.2 order 模块的 service 方法一一对应**：CREATE → `create.py`、UPDATE → `update.py`、5 个状态推进（START_COOKING/FINISH/MARK_DISPATCHED/CLOSE_PICKUP/CLOSE_DELIVERY）→ `lifecycle.py`、CANCEL → `cancel.py`；`query.py` 是查询不产生状态变更、不对应 action 枚举。便于按动作类型筛选与统计
5. **`api_token.token_hash` 加 UNIQUE 索引、`user.pin_hash` 不加**：技术方案 §4.2 已说明差异，本设计在索引层落地——token 校验是「客户端传明文 → 服务端 bcrypt.hash → SELECT 比对」可走索引，PIN 校验是「遍历 enabled 用户逐个 checkpw」无法走索引
6. **留痕表外键统一 `ON DELETE RESTRICT`**：技术方案未明确，本设计补充——即便关联业务记录被删，留痕必须保留可追溯

---

## 2. 订单状态机

订单是本系统状态最复杂的实体——7 个状态、8 个动作、3 种角色（店员 / 后厨 / 店主）参与的转换、不同状态下的库存联动差异。本节是 service 层（特别是 `lifecycle.py` / `cancel.py`）实现的权威依据。

原料、客户、配方等实体无真正意义上的状态机（数据为主、无状态流转），不在此节展开。

### 2.1 状态定义（7 个）

| 状态枚举 | 中文名 | 含义 | 进入条件 |
|---|---|---|---|
| `PENDING` | 待制作 | 订单已入库，等待后厨安排制作 | CREATE 动作 |
| `COOKING` | 制作中 | 后厨选择「现做」路径，正在按配方制作 | START_COOKING 动作 |
| `READY_FOR_PICKUP` | 待取 | 自取单蛋糕就绪，等待客户到店 | FINISH 动作（自取单） |
| `READY_FOR_DELIVERY` | 待配送 | 配送单蛋糕就绪，等待安排配送员 | FINISH 动作（配送单） |
| `DELIVERING` | 配送中 | 已交付配送员、送达途中 | MARK_DISPATCHED 动作 |
| `COMPLETED` | 已完成 | 客户取走 / 配送送达，订单终结 | CLOSE_PICKUP / CLOSE_DELIVERY 动作 |
| `CANCELLED` | 已取消 | 订单被取消，库存按既定策略处理 | CANCEL 动作 |

**终态**：`COMPLETED` 与 `CANCELLED` 是终态，不可再转换。

### 2.2 状态转换图（第一期）

```
                              ┌────────────────────────┐
                              │     （订单未创建）       │
                              └──────────┬─────────────┘
                                         │ CREATE
                                         ▼
                              ┌────────────────────────┐
                       ┌──────│       PENDING          │──────┐
                       │      │    （待制作）           │      │
                       │      └────────────────────────┘      │
                       │ CANCEL                  START_COOKING│
                       ▼                                  (现做路径)
                  ┌─────────┐                                ▼
                  │         │                    ┌────────────────────────┐
                  │CANCELLED│                    │       COOKING          │
                  │         │                    │      （制作中）         │
                  └─────────┘                    └────────────┬───────────┘
                      ▲                                      │ FINISH
                      │ CANCEL                      pickup_method 决定 ▼
                      │                                ┌─────┴──────┐
                      │                          自取  │            │  配送
                      │                                ▼            ▼
                      │             ┌──────────────────┐  ┌──────────────────────┐
                      │             │ READY_FOR_PICKUP │  │  READY_FOR_DELIVERY  │
                      │             │   （待取）        │  │    （待配送）         │
                      │             └────────┬─────────┘  └──────────┬───────────┘
                      │ CANCEL               │                       │ MARK_DISPATCHED
                      │                      │ CLOSE_PICKUP          ▼
                      │                      │              ┌──────────────────┐
                      │                      │              │    DELIVERING    │
                      │                      │              │   （配送中）      │
                      │                      │              └────────┬─────────┘
                      │                      │                       │ CLOSE_DELIVERY
                      │                      ▼                       ▼
                      │             ┌────────────────────────────────────┐
                      └─────────────│            COMPLETED               │
                                    │            （已完成）               │
                                    └────────────────────────────────────┘
```

**第一期仅现做路径**：所有订单从 `PENDING` 经 `START_COOKING` 进入 `COOKING`，再经 `FINISH` 进入 `READY_FOR_*`。第二期补「从成品库存扣减」路径（`PENDING` → `READY_FOR_*`，不经过 `COOKING`），详见 §2.5。

**`CANCEL` 是横切动作**：从任意非终态都可触发（图中只画了几条代表性 CANCEL 箭头，实际覆盖 `PENDING` / `COOKING` / `READY_FOR_PICKUP` / `READY_FOR_DELIVERY` / `DELIVERING` 五种状态）。

**`UPDATE` 不改状态**：仅在 `PENDING` 状态内做字段修改，不切状态、不出现在转换图上。

### 2.3 转换规则表（8 个 ORDER_ACTION）

每个动作的「前置状态 / 目标状态 / 触发角色 / 联动动作」清单。**所有动作必须经 service 层事务**（更新 `order.status` + 写 `order_status_log` + 触发库存/WebSocket 副作用），禁止绕过 service 直接改 `status`。

#### 2.3.1 CREATE（创建订单）

| 项 | 内容 |
|---|---|
| 前置状态 | （订单不存在）|
| 目标状态 | `PENDING` |
| 触发角色 | 店员（机器人下单，主流路径）／ 店主、店员（后端管理页面手动补录，少数场景） |
| service 入口 | `services/create.py` |
| 联动动作 | (1) 生成订单编号（`order_date` + `daily_seq`，按需求文档第二章 §4 编号规则）<br>(2) 缺料检测作为店员接单参考（不阻塞创建，结果通过回执返回）<br>(3) 客户档案备注异步补偿（订单备注同步入库 + 档案备注 FastAPI BackgroundTasks 异步提取，失败仅写 structlog WARN 日志）<br>(4) 写 `order_status_log`（action=CREATE, old_status=NULL, new_status=PENDING）<br>(5) 机器人下单时同步写 `bot_conversation_log`（step_type=ORDER_PERSISTED, order_id 回填）<br>(6) WebSocket 推送新订单到后厨屏 |
| 状态约束 | 无 |

#### 2.3.2 UPDATE（改单，仅字段修改）

| 项 | 内容 |
|---|---|
| 前置状态 | `PENDING`（**唯一允许改单的状态**，需求文档第二章故事 5 §319） |
| 目标状态 | `PENDING`（状态不变） |
| 触发角色 | 店员（机器人改单）／ 店主、店员（后端管理页面） |
| service 入口 | `services/update.py` |
| 联动动作 | (1) 计算 `change_diff`（结构见 §1.3.1，覆盖 7 类可改字段）<br>(2) 若涉及 `menu_id` / `quantity` 变化，按新值重算缺料检测并把差异告知店员（需求文档第二章故事 5 §313）<br>(3) 若涉及 `menu_id` / `quantity` 变化，价格字段按菜单最新价重算；若菜单价已变、回执显式提示（需求文档第二章故事 5 §329）<br>(4) 自取↔配送切换时补/清 `delivery_address`<br>(5) 写 `order_status_log`（action=UPDATE, old_status=PENDING, new_status=PENDING, change_diff=...）<br>(6) WebSocket 推送改单到后厨屏 |
| 状态约束 | `PENDING` 之外的状态拒绝改单，回执提示「订单已进入制作或交付环节，无法修改，如需调整请取消重下或与客户协商」 |

#### 2.3.3 START_COOKING（开始制作-现做路径）

| 项 | 内容 |
|---|---|
| 前置状态 | `PENDING` |
| 目标状态 | `COOKING` |
| 触发角色 | 后厨（后厨屏，需求文档第二章故事 6 §371 明确路径选择必须由后厨主动操作） |
| service 入口 | `services/lifecycle.py` |
| 联动动作 | (1) 锁定 `recipe_snapshot`（订单进入 COOKING 时锁定，按快照口径扣料）<br>(2) 填 `order.recipe_snapshot_id`<br>(3) 写 `order_status_log`（action=START_COOKING, old_status=PENDING, new_status=COOKING）<br>(4) WebSocket 推送状态变更到后厨屏与管理页面 |
| 状态约束 | 仅 `PENDING` 可触发；其他状态拒绝 |
| **第一期不实现** | 「从成品库存扣减」路径（`PENDING` → `READY_FOR_*`，不经过 `COOKING`）——第二期补，详见 §2.5 |

#### 2.3.4 FINISH（完工）

| 项 | 内容 |
|---|---|
| 前置状态 | `COOKING` |
| 目标状态 | `READY_FOR_PICKUP`（自取单）或 `READY_FOR_DELIVERY`（配送单），由 `order.pickup_method` 决定 |
| 触发角色 | 后厨（后厨屏，需求文档第六章故事 2） |
| service 入口 | `services/lifecycle.py` |
| 联动动作 | (1) 按 `recipe_snapshot` × `quantity` 扣减原料库存（写 `ingredient_stock_log`，source_type=FINISHED_GOODS_DEDUCT）<br>(2) 第一期简化口径：成品诞生 → 立刻被本订单匹配扣减（同一事务内完成，不进成品库存表）<br>(3) 写 `order_status_log`（action=FINISH, old_status=COOKING, new_status=READY_FOR_*）<br>(4) WebSocket 推送 |
| 状态约束 | 仅 `COOKING` 可触发 |
| **第二期扩展** | 拆为「成品诞生扣减」+「订单匹配扣减」两阶段事务（需求文档第十三章第 4 节取舍 1）；本期简化为一步 |

#### 2.3.5 MARK_DISPATCHED（标记已交配送）

| 项 | 内容 |
|---|---|
| 前置状态 | `READY_FOR_DELIVERY`（配送单专属） |
| 目标状态 | `DELIVERING` |
| 触发角色 | 后厨或店员（HTTP 端：后厨屏 / 后端管理页面均可调用）+ 店员（MCP 端：机器人 `mark_dispatched` 工具）——需求文档第十章 §3 核心层 (4)e 明确「后厨或店员」均可触发 |
| service 入口 | `services/lifecycle.py`（HTTP 与 MCP 共用同一 service 方法） |
| 联动动作 | (1) 写 `order_status_log`（action=MARK_DISPATCHED, old_status=READY_FOR_DELIVERY, new_status=DELIVERING）<br>(2) WebSocket 推送 |
| 状态约束 | 仅 `READY_FOR_DELIVERY` 可触发（自取单永远进不了这个状态） |

#### 2.3.6 CLOSE_PICKUP（客户到店取货关闭）

| 项 | 内容 |
|---|---|
| 前置状态 | `READY_FOR_PICKUP`（自取单专属） |
| 目标状态 | `COMPLETED` |
| 触发角色 | 后厨（后厨屏，需求文档第二章故事 3 §221） |
| service 入口 | `services/lifecycle.py` |
| 联动动作 | 按订单当前 `payment_status` 分两个子场景：<br>**子场景 A（未付订单，UNPAID）**：合并按钮「已付款 / 已交付」——(1) 支付状态变更为 `PAID`、记录 `payment_method` 与收款时间<br>(2) 状态切换 `COMPLETED`<br>(3) 写 `order_status_log`（action=CLOSE_PICKUP, old_status=READY_FOR_PICKUP, new_status=COMPLETED, note 含支付方式）<br>**子场景 B（已付订单，PAID）**：仅交付按钮——(1) 仅状态切换 `COMPLETED`<br>(2) 记录交付时间<br>(3) 写 `order_status_log`（action=CLOSE_PICKUP, old_status=READY_FOR_PICKUP, new_status=COMPLETED）<br>支付状态保持 `PAID` 不变，**不得触发二次收款** |
| 状态约束 | 仅 `READY_FOR_PICKUP` 可触发；前端按钮可用性由 `payment_status` 决定（未付只能点合并按钮、已付只能点交付按钮） |

#### 2.3.7 CLOSE_DELIVERY（配送订单送达关闭）

| 项 | 内容 |
|---|---|
| 前置状态 | `DELIVERING`（配送单专属） |
| 目标状态 | `COMPLETED` |
| 触发角色 | 店员（机器人为主入口 `close_delivery_order` MCP 工具）；后端管理页面同步提供作为「机器人功能镜像」（技术方案 §5.2 line 482） |
| service 入口 | `services/lifecycle.py` |
| 联动动作 | 按订单当前 `payment_status` 分两个子场景：<br>**子场景 A（送达付款，UNPAID）**：(1) 支付状态变更为 `PAID`、记录 `payment_method` 与收款时间<br>(2) 状态切换 `COMPLETED`<br>(3) 写 `order_status_log`（action=CLOSE_DELIVERY, old_status=DELIVERING, new_status=COMPLETED, note 含支付方式）<br>**子场景 B（下单时已付，PAID）**：(1) 状态切换 `COMPLETED`<br>(2) 记录送达时间<br>(3) 写 `order_status_log`（action=CLOSE_DELIVERY, old_status=DELIVERING, new_status=COMPLETED）<br>支付状态保持 `PAID` 不变 |
| 状态约束 | 仅 `DELIVERING` 可触发；若订单在 `READY_FOR_DELIVERY` 状态（未发出）需走取消流程，不在本动作范围 |

#### 2.3.8 CANCEL（取消订单）

| 项 | 内容 |
|---|---|
| 前置状态 | 任意非终态（`PENDING` / `COOKING` / `READY_FOR_PICKUP` / `READY_FOR_DELIVERY` / `DELIVERING`） |
| 目标状态 | `CANCELLED` |
| 触发角色 | 店员（机器人为主入口）；后端管理页面同步提供 |
| service 入口 | `services/cancel.py` |
| 联动动作 | 按订单当前状态分三种回滚策略（需求文档第二章故事 4 §277-285）：<br>**(1) `PENDING` / `COOKING`**：不扣库存（这两个状态原料尚未扣减、成品未诞生）<br>(2) 写 `order_status_log`（action=CANCEL, note 含取消原因）<br><br>**(3) `READY_FOR_PICKUP` / `READY_FOR_DELIVERY`**：蛋糕已成型、原料消耗已落账；按店员选择处理蛋糕实物——**第一期只有「报损废弃」一种**（写一条 `ingredient_stock_log` source_type=LOSS 记录损耗，需求文档第十三章第 4 节取舍明确）<br>(4) 写 `order_status_log`（action=CANCEL, note 含「报损废弃」与原因）<br><br>**(5) `DELIVERING`**：蛋糕已交付配送员、物理上无法回滚；系统**不自动处理库存**，仅置 `CANCELLED` 并在 note 留痕协商结果（截回配送 / 折价 / 补做等由店员与客户协商）<br>(6) 写 `order_status_log`（action=CANCEL, note 含协商结果） |
| 状态约束 | `COMPLETED` / `CANCELLED` 终态不可取消 |
| **配送员退回特殊情况** | `DELIVERING` 状态蛋糕被原样带回时，店员可在系统里手动选择「重新安排配送」（回到 `READY_FOR_DELIVERY`）、「客户自取」（回到 `READY_FOR_PICKUP`）或「入成品库存继续售卖」（按 `READY_FOR_*` 取消策略处理）——系统不自动判定退回原因。**第一期实现优先级低**，可作为第一刀切片之后的功能 |

### 2.4 状态约束矩阵（防误操作）

下表列出每个状态允许触发的动作集合，service 层应做前置校验、动作不允许时返回 400 + 业务错误码：

| 当前状态 | 允许的动作 |
|---|---|
| `PENDING` | UPDATE / START_COOKING / CANCEL |
| `COOKING` | FINISH / CANCEL |
| `READY_FOR_PICKUP` | CLOSE_PICKUP / CANCEL |
| `READY_FOR_DELIVERY` | MARK_DISPATCHED / CANCEL |
| `DELIVERING` | CLOSE_DELIVERY / CANCEL |
| `COMPLETED` | （无，终态） |
| `CANCELLED` | （无，终态） |

**注意**：`pickup_method` 进一步约束——自取单永远不会进入 `READY_FOR_DELIVERY` / `DELIVERING`，配送单永远不会进入 `READY_FOR_PICKUP`。`FINISH` 动作按 `pickup_method` 二选一决定目标状态。

### 2.5 第一期不实现 / 第二期扩展

| 项 | 第一期 | 第二期 |
|---|---|---|
| 「从成品库存扣减」路径 | 不实现（仅现做路径 `PENDING` → `COOKING` → `READY_FOR_*`） | 补 `PENDING` → `READY_FOR_*`（不经过 `COOKING`，按成品批次扣减） |
| `FINISH` 动作的事务粒度 | 简化为一步（成品诞生即扣减即匹配） | 拆为「成品诞生扣减」+「订单匹配扣减」两阶段（需求文档第十三章第 4 节取舍 1） |
| `STOCK_SOURCE` 枚举取值 | 4 种（PURCHASE / FINISHED_GOODS_DEDUCT / LOSS / INVENTORY_ADJUSTMENT） | 加 FINISHED_GOODS_RESTOCK + FINISHED_GOODS_MATCHED_DEDUCT，对应两阶段拆分 |
| 配送员退回的特殊处理 | 不实现（`DELIVERING` 取消仅留痕协商结果） | 补「重新安排配送 / 客户自取 / 入成品库存」三条路径（需求文档第二章故事 4 §285） |
| `CANCEL` 在 `READY_FOR_*` 的实物处理 | 只有「报损废弃」一种 | 补「入成品库存继续售卖」选项（需求文档第十三章第 4 节取舍） |
| 成品库存模块 | 不实现（第七章整体延后） | 补 `finished_goods_stock` 等表与流程 |

**实现策略**：第一期 service 层的状态机校验**仅按本节 §2.3-§2.4 实现**，第二期扩展时不重构已有状态、仅扩展 `STOCK_SOURCE` 取值集合与新增路径分支（详见需求文档第十三章第 4 节）。

---

## 3. API 契约概览

详细路径、请求体、响应体**不写在文档里**——FastAPI 的 Pydantic 模型在代码里定义、运行时自动生成 OpenAPI schema，前端用 `openapi-typescript` 同步生成 TS 类型，文档手抄一份会立刻过期。本节只写**接口设计的准则与约定**，让后端开发按准则实现、前端按约定调用，不需要每次为「字段叫什么」「错误码怎么命名」重新讨论。

### 3.1 契约生成与同步机制

```
backend/routers/*.py  ──Pydantic 模型──▶  FastAPI 运行时
                                              │
                                              ▼
                                      /openapi.json
                                              │
                          openapi-typescript  │  (前端 dev 期 npm script)
                                              ▼
                              frontend/src/api/types.gen.ts
                                              │
                                              ▼
                                  前端 import 与调用
```

- **单一可信源**：后端 Pydantic 模型。前端类型、OpenAPI 文档、`curl` 示例都从它派生
- **同步纪律**：任何 API 变更必须改 Pydantic 模型，commit 前跑 `npm run gen:api`（前端）拉取最新 `types.gen.ts`；CI 校验 `types.gen.ts` 与 `/openapi.json` 一致，不一致 fail build
- **不写 mock server**：第一期前端开发直接连真实后端（数据可用 seed 脚本灌），避免 mock 与真实契约漂移

### 3.2 路由模块化组织

后端 `routers/` 目录按业务模块拆，与 §1.1 表清单一一对应：

```
backend/app/routers/
├── auth.py              # /api/auth/*         PIN 登录、logout、me
├── orders.py            # /api/orders/*       订单 CRUD + 状态推进
├── customers.py         # /api/customers/*    客户档案
├── menu.py              # /api/menu/*         菜单 CRUD
├── ingredients.py       # /api/ingredients/*  原料档案 + 库存
├── recipes.py           # /api/recipes/*      配方 CRUD + 影响提示
├── purchases.py         # /api/purchases/*    采购登记
├── users.py             # /api/users/*        user 管理（店主后台，第二期补）
├── kitchen.py           # /api/kitchen/*      后厨屏专属 + WebSocket
├── internal.py          # /api/internal/*     OpenClaw Plugin 回写
└── health.py            # /health/*           健康检查（无鉴权）
```

每个 router 文件承担：路径声明、Pydantic schema 定义、调 service 层、统一响应包装。**不写业务逻辑**——业务逻辑全在 service 层（详见技术方案 §2.2）。

### 3.3 Pydantic schema 分层约定

每个模块的 schemas 按「用途」分四类，文件命名 `schemas/{module}.py`：

| 后缀 | 用途 | 示例 |
|---|---|---|
| `*Create` | POST 请求体 | `OrderCreate`、`CustomerCreate` |
| `*Update` | PATCH 请求体（**所有字段 Optional**，未传 = 不变） | `OrderUpdate`、`IngredientUpdate` |
| `*Response` | 单条响应体 | `OrderResponse`、`CustomerResponse` |
| `*ListItem` | 列表项（**裁剪字段**，不含详情字段，避免列表接口臃肿） | `OrderListItem`、`CustomerListItem` |

**约定**：
- `*Response` 与 ORM 模型不强制 1:1——`OrderResponse` 应包含前端展示需要的全部字段（含关联的 `customer.display_name`、`menu.name` 等），用 `from_attributes=True` 配合 service 层 join 一次取齐
- `*Update` 全字段 Optional，service 层用 `model_dump(exclude_unset=True)` 区分「字段未传」与「字段传 null」（后者表示显式清空，例如改单时 `delivery_address = null`）
- 严禁用同一个 schema 同时承担 Create 和 Update——字段必填性语义不同，混用必有 bug

### 3.4 通用响应包装

所有 HTTP API 与 MCP 工具统一返回：

```python
class ApiResponse(BaseModel, Generic[T]):
    data: T | None = None
    error: ApiError | None = None

class ApiError(BaseModel):
    code: str          # 大写下划线，机器可读
    message: str       # 中文，给操作者看
    details: dict = {} # 可选，结构化补充（如缺料列表）
```

**纪律**：
- 成功：`data` 必填、`error = null`、HTTP 200
- 业务失败：`data = null`、`error.code` 是机器可读错误码、HTTP 400/404/409
- 鉴权失败：HTTP 401（未登录 / session 失效 / Bearer Token 无效）/ 403（已登录但角色不够）/ 429（PIN 锁定或限流）
- 系统错误：HTTP 500 + `INTERNAL_ERROR`，堆栈写 structlog、不暴露给前端

### 3.5 列表接口的 query 参数约定

所有列表接口（`GET /api/orders`、`GET /api/customers` 等）统一约定：

| 参数 | 类型 | 说明 |
|---|---|---|
| `page` | int | 页码，从 1 开始，默认 1 |
| `page_size` | int | 每页条数，默认 20，最大 100（防前端误传大值拖垮 DB） |
| `sort` | str | 排序字段，格式 `field` 或 `-field`（`-` 前缀表降序），如 `-created_at` |
| `q` | str | 模糊搜索（按模块定义可搜字段，如订单编号、客户名、商品名） |
| 其他过滤 | 各模块自定义 | 如订单列表的 `status`、`date_from`、`date_to` |

响应统一为：

```python
class PaginatedData(BaseModel, Generic[T]):
    items: list[T]
    total: int        # 满足过滤条件的总数（用于前端分页器）
    page: int
    page_size: int
```

### 3.6 错误码命名规范

错误码按模块前缀分组，便于前端按 code 路由处理：

| 模块前缀 | 示例 |
|---|---|
| `AUTH_*` | `AUTH_INVALID_PIN`、`AUTH_LOCKED`、`AUTH_SESSION_EXPIRED` |
| `ORDER_*` | `ORDER_NOT_FOUND`、`ORDER_STATE_FORBIDDEN`、`ORDER_DAILY_SEQ_CONFLICT` |
| `STOCK_*` | `STOCK_INSUFFICIENT`、`STOCK_LOG_FORBIDDEN` |
| `CUSTOMER_*` | `CUSTOMER_NOT_FOUND`、`CUSTOMER_PHONE_DUPLICATE` |
| `MENU_*` | `MENU_NOT_FOUND`、`MENU_OFFLINE` |
| `RECIPE_*` | `RECIPE_NOT_FOUND`、`RECIPE_REFERENCED_IN_PENDING_ORDERS` |
| `INTERNAL_ERROR` | 兜底系统错误（不暴露细节） |

错误码集中在 `backend/app/constants/error_codes.py` 维护，新增错误码必须在这里登记，避免散落在各 router。

### 3.7 WebSocket 事件协议

后厨屏订阅 `wss://.../api/kitchen/ws`（session cookie 鉴权，连接时校验失败立即 close）。后端推送的事件类型：

| event | 触发时机 | payload |
|---|---|---|
| `order.created` | CREATE 动作完成 | 完整订单（OrderListItem） |
| `order.updated` | UPDATE 动作完成 | 订单 id + change_diff |
| `order.state_changed` | 状态推进（START_COOKING / FINISH / MARK_DISPATCHED / CLOSE_*） | 订单 id + old_status + new_status |
| `order.cancelled` | CANCEL 动作完成 | 订单 id + 取消原因 |

**协议约定**：
- 每条消息为 JSON：`{ "event": "order.created", "payload": {...}, "ts": "2026-06-29T10:00:00Z" }`
- 前端连接后发送 `{ "event": "subscribe", "payload": { "client_type": "kitchen" } }` 完成握手；后端只对 `client_type=kitchen` 的连接推流（防止管理页面误连）
- 心跳：前端每 30s 发 `{ "event": "ping" }`，后端 60s 无心跳断开
- 重连：前端指数退避（1s → 2s → 4s → ... 上限 30s），重连成功后主动调一次 `GET /api/kitchen/today` 拉取期间错过的更新（不依赖 WebSocket replay）
- 多 worker 部署：技术方案 §8 决策——Uvicorn 单 worker，本期不上 Redis Pub/Sub

### 3.8 内部回写端点（`/api/internal/*`）

仅供 OpenClaw Plugin 等程序访问，**不接受 session cookie、只接受 Bearer Token**：

- `POST /api/internal/bot-conversation-log`：Plugin 回写对话留痕（session_id / step_type / payload / operator_wxid / order_id）

Caddy 路径过滤层面 `internal` 段对内网/同机放开、对外不暴露（如果 OpenClaw 与 backend 同机，可绑定 127.0.0.1；如果分机，靠 Bearer Token 鉴权）。

---

## 4. 前端页面与 ASCII 线框

前端两个独立入口：**后端管理页面**（店主/店员用，PC 浏览器，session 8 小时）与**后厨屏**（后厨用，墙上平板，session 90 天）。两者是不同的 Vue 应用、独立构建、独立部署到 Caddy 静态目录的不同路径。

技术栈：Vue 3 + Composition API + Element Plus + Vite + Pinia + TypeScript（技术方案 §1.1）。

线框图只表达**信息结构与关键交互**，不抠视觉细节（颜色/字号由 Element Plus 默认主题与少量自定义 CSS 承担，不做像素级 mockup）。

### 4.1 后端管理页面（admin）

路径前缀 `/admin/*`，登录后进入主框架。

#### 4.1.1 登录页 `/admin/login`

```
┌──────────────────────────────────────────────────┐
│                                                  │
│              小蛋糕店业务管理系统                  │
│              ─── 后端管理 ───                     │
│                                                  │
│         ┌──────────────────────────┐             │
│         │  PIN 码                  │             │
│         │  ┌──┬──┬──┬──┬──┬──┐    │             │
│         │  │  │  │  │  │  │  │    │             │
│         │  └──┴──┴──┴──┴──┴──┘    │             │
│         │                          │             │
│         │       [ 登 录 ]          │             │
│         └──────────────────────────┘             │
│                                                  │
│         client_type: admin（隐藏字段）            │
└──────────────────────────────────────────────────┘
```

交互要点：
- 6 位数字输入框，自动 focus、自动跳下一格、输满自动提交
- PIN 错 5 次锁 10 分钟，期间提交返回 429 + `AUTH_LOCKED`，前端展示「请在 X 分钟后再试」
- 登录成功跳 `/admin/orders`（订单列表作为首页，符合店主高频场景）

#### 4.1.2 主框架 `/admin/*`

```
┌──────────────────────────────────────────────────────────────────┐
│  小蛋糕店 · 管理后台        店主张三 ▾    [退出]                  │
├──────────┬───────────────────────────────────────────────────────┤
│          │                                                       │
│  📋 订单 │                                                       │
│  👥 客户 │                                                       │
│  🎂 菜单 │            ← 各模块内容区（右侧）                       │
│  📦 原料 │                                                       │
│  📒 配方 │                                                       │
│  🛒 采购 │                                                       │
│          │                                                       │
│  ─────   │                                                       │
│  👤 用户 │                                                       │
│  ⚙ 设置  │                                                       │
└──────────┴───────────────────────────────────────────────────────┘
```

- 左侧 Element Plus `Menu`，按业务域分组；底部「用户 / 设置」第二期补
- 顶部右侧显示当前登录 `display_name`，操作留痕的来源
- session 失效自动跳登录页（HTTP 401 拦截器统一处理）

#### 4.1.3 订单列表 `/admin/orders`

```
┌──────────────────────────────────────────────────────────────────┐
│  订单管理                                                         │
│                                                                  │
│  状态 [全部 ▾]  日期 [2026-06-29]  搜索 [订单号/客户名____] [🔍] │
│  取货方式 [全部 ▾]                                               │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │订单号    客户     商品       数量 取货 时间       状态  操作│ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │0629-001 张三妈 草莓蛋糕(8寸)  1  自取 18:00     待制作  …  │ │
│  │0629-002 李四    巧克力(6寸)   2  配送 19:00     制作中  …  │ │
│  │0629-003 王五    慕斯(8寸)     1  自取 明天 10:00 待取   …  │ │
│  │...                                                          │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                            ‹ 1 2 3 ... 10 ›      │
└──────────────────────────────────────────────────────────────────┘
```

交互要点：
- 列表项右侧 `…` 菜单按当前 `status` 动态显示可执行动作（参照 §2.4 矩阵）：待制作显示「改单 / 取消 / 开始制作」、制作中显示「完工」、待配送显示「标记已交配送」等
- 点订单号或行进入详情页
- 「搜索」框模糊匹配订单号、客户名、客户电话；走 `q` 参数

#### 4.1.4 订单详情 `/admin/orders/:order_no`

```
┌──────────────────────────────────────────────────────────────────┐
│  ← 返回    订单 0629-001                                          │
│                                                                  │
│  状态：待制作          [改单] [取消] [开始制作]                  │
│                                                                  │
│  ┌──────────────────────┬───────────────────────────────────────┐│
│  │ 客户    张三妈        │ 商品      草莓蛋糕（8寸）             ││
│  │ 电话    138****1234   │ 数量      1                          ││
│  │ 地址    ——（自取）    │ 单价      ¥168                       ││
│  │ 备注    不要坚果       │ 合计      ¥168                       ││
│  │         （过敏）       │ 取货方式  自取                       ││
│  │                      │ 取货时间  2026-06-29 18:00            ││
│  │                      │ 支付状态  未付                        ││
│  └──────────────────────┴───────────────────────────────────────┘│
│                                                                  │
│  操作历史                                                         │
│  ─ 2026-06-28 14:30  店员小赵（机器人）创建订单                  │
│  ─ 2026-06-28 14:31  改单：商品从「巧克力」改为「草莓」（+¥18）│
│  ─ ...                                                           │
│                                                                  │
│  对话留痕（机器人下单时）                                         │
│  ─ 店员原始消息：客户张三妈订一个 8 寸的，要草莓...              │
│  ─ 识别结果：商品=草莓蛋糕(8寸) 数量=1 客户=张三妈 备注=不要坚果│
│  ─ 机器人回执：已下单 0629-001，明天 18:00 自取                  │
│  ─ ...                                                           │
└──────────────────────────────────────────────────────────────────┘
```

- 顶部按钮组按 `status` 动态变化（参照 §2.4 矩阵）
- 「改单」打开弹窗（4.1.5），改单留痕用 change_diff 显示
- 「操作历史」从 `order_status_log` 拉
- 「对话留痕」从 `bot_conversation_log` 拉（按 session_id 聚合，订单创建时回填 order_id 关联）

#### 4.1.5 改单弹窗（订单详情点「改单」）

```
┌──────────────────────────────────────┐
│  改单 · 0629-001                  ✕ │
├──────────────────────────────────────┤
│  商品     [草莓蛋糕(8寸) ▾]          │
│  数量     [   1   ] [+] [-]          │
│  取货方式 (●) 自取  ( ) 配送         │
│  取货时间 [2026-06-29 18:00]         │
│  配送地址（配送单才显示）             │
│           [槐树街 12 号___]          │
│  备注     [不要坚果（过敏）______]   │
│                                      │
│  ⚠ 改单后差异：                       │
│    商品 巧克力 → 草莓 (+¥18)         │
│    缺料检测：当前库存可制作（√）     │
│                                      │
│             [取消]  [确认改单]       │
└──────────────────────────────────────┘
```

- 实时计算 change_diff 并预览
- 改 `menu_id` / `quantity` 时实时跑缺料检测并展示（不阻塞、仅提示）
- 仅 `status = PENDING` 可进入此弹窗；其他状态后端拒绝，前端按 §2.4 矩阵不显示「改单」按钮

#### 4.1.6 菜单管理 `/admin/menu`

```
┌──────────────────────────────────────────────────────────────────┐
│  菜单管理                                          [+ 新增商品]   │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │商品          售价   保质期 关联配方 状态 操作               │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │草莓蛋糕(8寸) ¥168   3 天  草莓v2   在售  编辑 / 下架        │ │
│  │巧克力(6寸)   ¥98    3 天  巧克力v1 在售  编辑 / 下架        │ │
│  │慕斯(8寸)     ¥188   2 天  ——      下架  编辑 / 上架        │ │
│  │...                                                          │ │
│  └────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

- 「关联配方」可空（先建商品、再建配方）
- 「下架」不删数据、置 `enabled=false`；已下架商品不出现在机器人下单选项，但历史订单的 `recipe_snapshot` 不受影响（快照锁定）

#### 4.1.7 原料管理 `/admin/ingredients`

```
┌──────────────────────────────────────────────────────────────────┐
│  原料管理                                       [+ 新增原料]      │
│                                                                  │
│  库存预警 [显示预警 ▾]                                           │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │原料        单位  当前库存 安全线 单价 操作                  │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │低筋面粉    kg    2.5 ⚠     5     12.0 详情 / 入库 / 报损    │ │
│  │鸡蛋        个    80        30    1.0  详情 / 入库 / 报损    │ │
│  │草莓        kg    3.0       2     35.0 详情 / 入库 / 报损    │ │
│  │...                                                          │ │
│  └────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

- 库存低于安全线的行标 ⚠、首列红字（`current_qty < safety_stock`）
- 「入库」打开弹窗：数量 + 单价 + 备注 → 写 `ingredient_stock_log` source_type=PURCHASE
- 「报损」打开弹窗：数量 + 原因 → 写 `ingredient_stock_log` source_type=LOSS
- 「详情」进入原料档案 + 库存变动流水

#### 4.1.8 配方管理 `/admin/recipes`

```
┌──────────────────────────────────────────────────────────────────┐
│  配方管理                                       [+ 新增配方]      │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │配方          关联商品       版本  操作                      │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │草莓蛋糕(8寸) 草莓蛋糕(8寸)  v2   编辑 / 改影响提示 / 历史   │ │
│  │巧克力(6寸)   巧克力(6寸)    v1   编辑 / 改影响提示 / 历史   │ │
│  │...                                                          │ │
│  └────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

- 「改影响提示」按钮：点击后弹窗列出「待制作 + 制作中」状态、使用该配方的订单及缺料检测变化（参照技术方案 §8 决策 5——口径与第五章 §5-2 承诺量一致）
- 「历史」查看配方版本历史（同 menu_id 的所有版本）

配方编辑表单（弹窗或独立页）：

```
┌──────────────────────────────────────────────────────────────┐
│  配方编辑 · 草莓蛋糕(8寸)                                  ✕ │
├──────────────────────────────────────────────────────────────┤
│  关联商品  [草莓蛋糕(8寸) ▾]                                 │
│                                                              │
│  原料清单（每行：原料 + 用量 + 单位）                         │
│  ┌────────────────────┬──────┬─────┐                         │
│  │ 低筋面粉            │ 0.3  │ kg  │ ✕                       │
│  │ 鸡蛋                │ 6    │ 个  │ ✕                       │
│  │ 草莓                │ 0.5  │ kg  │ ✕                       │
│  └────────────────────┴──────┴─────┘                         │
│  [+ 添加原料]                                                │
│                                                              │
│  制作步骤（steps JSONB，按步骤号展示）                        │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 1. 蛋黄蛋白分离，蛋黄加糖打发                            │ │
│  │ 2. 筛入低筋面粉，拌匀                                    │ │
│  │ 3. ...                                                  │ │
│  └────────────────────────────────────────────────────────┘ │
│  [+ 添加步骤]                                                │
│                                                              │
│                              [取消]  [保存]                  │
└──────────────────────────────────────────────────────────────┘
```

- 编辑保存即生成新版本（同 `menu_id` 自增 version）
- 已被订单 `recipe_snapshot` 引用的旧版本不可物理删除（外键约束）

#### 4.1.9 采购登记 `/admin/purchases`

```
┌──────────────────────────────────────────────────────────────────┐
│  采购登记                                       [+ 新建采购单]   │
│                                                                  │
│  日期 [2026-06-29]  供应商 [全部 ▾]                              │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │采购单号    日期       供应商  总额    明细数 操作           │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │P0629-001   2026-06-29 沃尔玛 ¥356.5  5      详情 / 作废     │ │
│  │P0628-002   2026-06-28 山姆   ¥892.0  12     详情 / 作废     │ │
│  └────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

新建采购单弹窗：

```
┌──────────────────────────────────────────────────────────┐
│  新建采购单                                            ✕ │
├──────────────────────────────────────────────────────────┤
│  供应商  [沃尔玛____]   日期 [2026-06-29]                │
│                                                          │
│  明细                                                    │
│  ┌──────────────┬──────┬──────┬────────┐                 │
│  │ 原料         │ 数量 │ 单价 │ 小计   │                 │
│  ├──────────────┼──────┼──────┼────────┤                 │
│  │ 低筋面粉     │ 10   │ 12.0 │ 120.0  │ ✕               │
│  │ 鸡蛋         │ 100  │ 1.0  │ 100.0  │ ✕               │
│  └──────────────┴──────┴──────┴────────┘                 │
│  [+ 添加原料]                                            │
│                                                          │
│  合计：¥220.0                                            │
│                                                          │
│  ☑ 入库（保存即写 ingredient_stock_log）                 │
│                                                          │
│                              [取消]  [保存]              │
└──────────────────────────────────────────────────────────┘
```

- 保存时若勾选「入库」，同事务写 `purchase` + 多条 `ingredient_stock_log`（source_type=PURCHASE）
- 「作废」逻辑上冲红，但已写的 stock_log 不删（保留审计轨迹，第二期评估是否补冲红条目）

#### 4.1.10 客户档案 `/admin/customers`

```
┌──────────────────────────────────────────────────────────────────┐
│  客户档案          搜索 [客户名/电话/wxid______] [🔍]            │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │客户       电话           历史订单 备注数 操作              │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │张三妈     138****1234    12      3      详情               │ │
│  │李四       139****5678    4       0      详情               │ │
│  └────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

详情页（4.1.11）：

```
┌──────────────────────────────────────────────────────────────────┐
│  ← 返回   客户档案 · 张三妈                                       │
│                                                                  │
│  ┌──────────────────────┬───────────────────────────────────────┐│
│  │ wxid       wxid_abc  │ 电话 138****1234 [+]                  ││
│  │ 首次下单   2025-12-01│ 地址 槐树街 12 号 [+]                 ││
│  │ 累计订单   12 单     │ 备注 不要坚果（过敏） ⚠ 高优先级 [+]  ││
│  │ 累计消费   ¥2,156    │      喜欢甜一点                       ││
│  └──────────────────────┴───────────────────────────────────────┘│
│                                                                  │
│  最近订单                                                         │
│  0629-001 草莓蛋糕(8寸) ¥168 待制作 ...                          │
│  0625-008 巧克力(6寸)   ¥98  已完成 ...                          │
│  ...                                                             │
└──────────────────────────────────────────────────────────────────┘
```

- 备注「⚠ 高优先级」标记：备注 `priority=high`（如过敏、忌口），下单时机器人接单流程强制提示店员
- 「[+]」按钮展开新增电话/地址/备注的弹窗

### 4.2 后厨屏（kitchen）

路径前缀 `/kitchen/*`，**独立 Vue 应用**、部署到独立子路径（避免与管理页面共享 bundle）。设计目标是「墙上平板常亮、一眼看清待办、最少点击」。

#### 4.2.1 登录页 `/kitchen/login`

```
┌──────────────────────────────────────────────────┐
│                                                  │
│              ─── 后厨屏 ───                      │
│                                                  │
│         ┌──────────────────────────┐             │
│         │  PIN 码                  │             │
│         │  ┌──┬──┬──┬──┬──┬──┐    │             │
│         │  │  │  │  │  │  │  │    │             │
│         │  └──┴──┴──┴──┴──┴──┘    │             │
│         │       [ 登 录 ]          │             │
│         └──────────────────────────┘             │
│                                                  │
│         client_type: kitchen（隐藏字段）          │
│         session 有效期 90 天                      │
└──────────────────────────────────────────────────┘
```

- 与 admin 登录页同样的 PIN 输入，但 `client_type=kitchen`、session 90 天（避免天天重输）
- 登录成功跳 `/kitchen/board`（看板主页）

#### 4.2.2 看板主页 `/kitchen/board`

```
┌────────────────────────────────────────────────────────────────────────────┐
│  后厨看板    2026-06-29    [🔔 提示音开关 ●]    王师傅 ▾    [退出]          │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  待制作（3）              制作中（2）          待取/待配送（4）              │
│  ┌────────────────┐     ┌────────────────┐  ┌────────────────┐            │
│  │0629-005 18:00  │     │0629-002 19:00  │  │0629-001 18:00  │            │
│  │草莓8寸 ×1      │     │巧克力6寸 ×2    │  │草莓8寸 ×1      │            │
│  │自取 ⚠备注       │     │配送 →槐树街12号│  │自取 已付        │            │
│  │⚠ 不要坚果       │     │[完工]          │  │[客户已取]      │            │
│  │[开始制作]       │     └────────────────┘  └────────────────┘            │
│  └────────────────┘     ┌────────────────┐  ┌────────────────┐            │
│  │0629-006 18:30  │     │0629-004 19:30  │  │0629-003 20:00  │            │
│  │慕斯8寸 ×2      │     │草莓8寸 ×1      │  │巧克力6寸 ×2    │            │
│  │配送            │     │自取            │  │配送 已交       │            │
│  │[开始制作]       │     │[完工]          │  │[已送达]        │            │
│  └────────────────┘     └────────────────┘  └────────────────┘            │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

- 三列对应三种状态：待制作 / 制作中 / 待取与待配送（按取货时间升序）
- 每张卡片自上而下：订单号 + 取货时间 / 商品（数量）/ 取货方式 + 备注高亮 / 主操作按钮
- **备注高亮**：客户备注含「过敏」「忌口」等高优先级关键词时卡片头部红色横条 + ⚠ 图标，确保后厨第一眼看到
- **新订单**：WebSocket `order.created` 事件触发卡片从顶部插入、播放提示音（管理面板可关）
- **状态切换**：点按钮即时调 API，成功后卡片横向滑入下一列（前端动画 ≤ 300ms），失败 toast 提示
- ** Wake Lock**：页面常驻期间请求 `navigator.wakeLock('screen')`，防止平板自动息屏（技术方案 §8 决策 7）
- **重连**：WebSocket 断开指数退避重连，UI 顶部显示「离线重连中」横幅，重连成功主动 `GET /api/kitchen/today` 拉一次

#### 4.2.3 配方查看（卡片点商品名）

```
┌──────────────────────────────────────┐
│  草莓蛋糕(8寸) 配方（v2）          ✕ │
├──────────────────────────────────────┤
│  原料清单（×2 数量已乘好）            │
│  ┌──────────────────────────────┐   │
│  │ 低筋面粉    0.6 kg            │   │
│  │ 鸡蛋        12 个             │   │
│  │ 草莓        1.0 kg            │   │
│  └──────────────────────────────┘   │
│                                      │
│  制作步骤                            │
│  1. 蛋黄蛋白分离...                  │
│  2. 筛入低筋面粉...                  │
│  3. ...                              │
│                                      │
│              [关闭]                  │
└──────────────────────────────────────┘
```

- 弹窗显示，原料按 `recipe_snapshot`（订单进入 COOKING 时锁定的快照）展示
- 制作中卡片点商品名才显示快照版本；待制作状态点商品名显示当前最新配方（让后厨提前看预期）

#### 4.2.4 客户到店取货（待取卡片点「客户已取」）

按订单 `payment_status` 弹两种 UI：

```
未付订单（合并按钮）              已付订单（仅交付）
┌──────────────────────┐        ┌──────────────────────┐
│ 0629-001 客户已取    ✕│        │ 0629-001 客户已取    ✕│
├──────────────────────┤        ├──────────────────────┤
│ 应收 ¥168             │        │ 已付 ¥168            │
│ 支付方式              │        │                      │
│ (●) 现金  ( ) 微信    │        │  确认客户取走蛋糕？   │
│ ( ) 支付宝            │        │                      │
│                      │        │                      │
│     [确认收款并交付]  │        │       [确认交付]     │
└──────────────────────┘        └──────────────────────┘
```

- 与 §2.3.6 状态机两个子场景对齐
- 「确认收款并交付」同事务：状态切 COMPLETED + 支付状态切 PAID + 记录收款时间与方式
- 「确认交付」仅状态切换，支付状态保持不变（禁止二次收款）

#### 4.2.5 配送订单送达关闭（待配送卡片点「已送达」）

机器人是主入口（`close_delivery_order` MCP 工具），后厨屏只在配送员回店报备时人工补打。UI 与 4.2.4 同构，按 `payment_status` 分「送达付款」/「已付送达」两个子场景。

### 4.3 通用前端约定

#### 4.3.1 API 客户端封装

```typescript
// frontend/admin/src/api/client.ts（kitchen 应用同样一份）
const client = axios.create({ baseURL: '/api', withCredentials: true })

client.interceptors.response.use(
  res => res.data,
  err => {
    if (err.response?.status === 401) router.push('/login')
    return Promise.reject(err.response?.data?.error ?? err)
  }
)
```

- session cookie 浏览器自动管理，前端不读不写 token（避免 XSS 风险）
- 401 拦截器统一跳登录页
- 错误统一抛出 `ApiError` 形态，调用方按 `error.code` 处理

#### 4.3.2 操作留痕字段的统一来源

所有「操作人」展示（订单详情的操作历史、库存变动的操作人列）从后端返回的 `operator.display_name` 取，前端**不允许**自报操作人 header（废弃 v1.0 的 `X-Kitchen-Operator`，详见技术方案 §6.2）。

#### 4.3.3 提示音与 Wake Lock 设置面板

后厨屏登录后右上角「🔔 提示音开关」与「息屏锁定开关」：

- 提示音：默认开，存 `localStorage`，新订单 `order.created` 事件触发播放 `notification.mp3`（技术方案 §8 决策 9——浏览器原生 Web Speech API 在第二期可选叠加语音播报）
- Wake Lock：默认开，存 `localStorage`，失焦或可见性切换时自动重新申请

---
